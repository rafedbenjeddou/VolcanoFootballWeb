{% extends 'base-back.html.twig' %}
{% block content %}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">

                <h4 class="card-title">Liste Des reservations</h4>
                <table id="datatable-buttons" class="table table-striped table-bordered dt-responsive nowrap" style="border-collapse: collapse; border-spacing: 0; width: 100%;">
                    <thead>
                    <tr>
        <th>ID</th>
        <th>date debut</th>
        <th>date fin</th>
        <th>hebergement</th>
        <th>idClient</th>
        <th>Update</th>
        <th>Delete</th>
    </tr>
    <tr {% for r in reservation %}>
        <td>{{ r.id }}</td>
        <td>{{ r.dateDebut|date('d-m-Y') }}</td>
        <td>{{ r.dateFin|date('d-m-Y') }}</td>
        <td>{{ r.hebergement.NomH }}</td>
        <td>{{ r.idClient }}</td>
        <td><a href="{{ path('UpdateR',{'id':r.id}) }}">Update</a></td>
        <td><a href="{{ path('deleteR',{'id':r.id}) }}">Delete</a></td>


    </tr{% endfor %}>

</tbody>


</table>
</div>
</div>
</div> <!-- end col -->
</div> <!-- end row -->
{% endblock %}
